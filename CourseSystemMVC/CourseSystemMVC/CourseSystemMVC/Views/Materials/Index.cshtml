@model IEnumerable<CourseSystemMVC.Models.Material>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Materials</h2>

<p>
    @Html.ActionLink("Add material", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FilePath)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lesson.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FilePath)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Lesson.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaterialID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaterialID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaterialID })
        </td>
    </tr>
}

</table>
